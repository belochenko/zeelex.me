# Local Setup

Use Yarn to install dependencies and run the project:

```bash
yarn install
yarn dev
```

# Testing

Run the available quality checks locally:

```bash
yarn lint
```

# Build

Create a production build with:

```bash
yarn build
```

